export interface ProductPayload {
  input: {
    amount: string;
    description: string;
    email: string;
    name: 'contact@leveor.xyz';
    price: string;
    productId: string;
    unit: string;
  };
  options: {};
}
